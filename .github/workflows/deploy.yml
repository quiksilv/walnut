name: Deploy
on:
  push:
    branches: [ "main"]
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Deploy to Cloudflare
        run: echo "Deploy to Cloudflare"
      - name: Deploy to Amazon EC2
        run: echo "Deploy to Amazon EC2"
